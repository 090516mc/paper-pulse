name: Fetch Papers

on:
  # Weekly at 00:00 UTC on Sunday
  schedule:
    - cron: '0 0 * * 0'

  # Manual trigger
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-and-summarize:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Fetch and summarize papers
        id: fetch
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENAI_API_BASE }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
        run: |
          python scripts/main.py

      - name: Get current date
        id: date
        run: echo "date=$(date -u +'%Y-%m-%d %H:%M UTC')" >> $GITHUB_OUTPUT

      - name: Get repository info
        id: repo
        run: |
          echo "owner=${{ github.repository_owner }}" >> $GITHUB_OUTPUT
          echo "repo=${{ github.event.repository.name }}" >> $GITHUB_OUTPUT

      - name: Send email notification with PDF attachment
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: Paper Pulse Weekly Report - ${{ steps.date.outputs.date }}
          to: ${{ secrets.EMAIL_TO }}
          from: Paper Pulse Bot <${{ secrets.EMAIL_USERNAME }}>
          body: file://data/email_report.txt
          attachments: data/weekly_report.pdf

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/ feed.xml config.js
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update papers data - $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
            git push
          fi
